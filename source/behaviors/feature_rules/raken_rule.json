{
	"format_version": "1.13.0",

	"minecraft:feature_rules": {
		"description": {
			"identifier": "zalcyan:raken_rule",
			"places_feature": "zalcyan:raken_var_weighted"
		},

		"conditions": {
			"placement_pass": "first_pass",

			"minecraft:biome_filter": {
				"all_of": [
					{
						"any_of": [
							{
								"test": "has_biome_tag",
								"operator": "==",
								"value": "swamp"
							}
						]
					}
				]
			}
		},

		"distribution": {
			"iterations": 256,

			"x": {
				"distribution": "fixed_grid",
				"extent": [0, 15]
			},
			"z": {
				"distribution": "fixed_grid",
				"extent": [0, 15]
			},
			"y": "(q.noise(v.worldx / 512, v.worldz / 512) + q.noise(v.worldx, v.worldz) * 0.05 < -0.4 && (query.heightmap(variable.worldx, variable.worldz) >= 64)) ? (q.heightmap(v.worldx, v.worldz) - 1) : -70"
		}
	}
}